import path from 'path';
import rootDir from '../util/path';

export const PRODUCTS_PATH = path.join(rootDir, 'data', 'products.json');
export const CART_PATH = path.join(rootDir, 'data', 'cart.json');
